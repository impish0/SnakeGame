## Example environment for server
# CORS_ORIGIN can be a single origin, a comma-separated list, or * to allow all.
# e.g. CORS_ORIGIN=https://example.com
# e.g. CORS_ORIGIN=https://example.com,http://192.168.1.217:5173
CORS_ORIGIN=

# PUBLIC_API_URL: optional. When set, the client will use this API base (useful
# when serving the client from a different host or via a CDN). If unset, the
# server will return the request origin as the API base.
PUBLIC_API_URL=

# Server port
PORT=4000

DATABASE_URL="file:./dev.db"